---
import SocialList from "@/components/SocialList.astro";
import PostPreview from "@/components/blog/PostPreview.astro";
import HeroImage from "@/components/landing/HeroImage.astro";
import { getAllPosts, sortMDByDate } from "@/data/post";
import PageLayout from "@/layouts/Base.astro";
import { getCollection } from "astro:content";
import { Icon } from "astro-icon/components";

async function getAllLabNotes() {
	const allLabNotes = await getCollection("labNotes");
	return allLabNotes;
}

const MAX_POSTS = 10;
const allPosts = await getAllPosts();
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

const allLabNotes = await getAllLabNotes();
console.log("🚀 ~ allLabNotes:", allLabNotes);
---

<PageLayout meta={{ title: "Home" }}>
	<div class="grid grid-cols-1 gap-4 md:grid-cols-7">
		<section class="col-span-5">
			<HeroImage />
			<!-- <SocialList /> -->
			<section aria-label="Blog post list" class="mt-16">
				<h2 class="mb-4">Posts</h2>
				<ul class="space-y-4">
					{
						allPostsByDate.map((p) => (
							<li class="grid gap-2 sm:grid-cols-[auto_1fr] sm:[&_q]:col-start-2">
								<PostPreview post={p} />
							</li>
						))
					}
				</ul>
			</section>
		</section>
		<!-- placeholder section -->
		<section class="col-span-2 h-full"></section>

		<section
			class="absolute right-0 h-full max-w-[25%] overflow-y-scroll bg-bgColour-muted/30 md:top-20"
		>
			<div class="h-full p-2 outline outline-2 outline-black/10 ring-1 ring-inset ring-gray-900/10">
				<div class="bg-gray-900/2.5 rounded-xl p-2 shadow-md ring-1 ring-inset ring-gray-900/10">
					<h2 class="flex flex-col items-center gap-2">
						<Icon class="size-10" name="flask" />
						Lab Notes
					</h2>
					<ul>
						{allLabNotes.map((l) => <li>{l.data.title}</li>)}
					</ul>
				</div>
			</div>
		</section>
	</div>
</PageLayout>
